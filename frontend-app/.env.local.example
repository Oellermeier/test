# Backend-URL für Client-seitige Rewrites (und next.config.ts Proxy-Ziel)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Backend-URL für Server Components (direkt, ohne Proxy)
# In Docker: http://backend:3000
API_URL=http://localhost:3000
